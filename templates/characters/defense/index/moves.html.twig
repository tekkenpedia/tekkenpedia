<div class="row">
    {% for move in section.moves.toArray() -%}
        {% include 'characters/defense/move.html.twig' with {character: character, move: move, moveClass: "col-12 col-sm-6 col-md-6 col-lg-3 mt-3"} only %}
    {% endfor %}
</div>
