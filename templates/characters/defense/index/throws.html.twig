<div class="row">
    {% for throw in section.throws.toArray() -%}
        {% include 'characters/defense/throw.html.twig' with {character: character, throw: throw, throwClass: "col-12 col-sm-6 col-md-6 col-lg-3 mt-3"} only %}
    {% endfor %}
</div>
