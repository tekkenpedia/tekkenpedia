#!/usr/bin/env bash

set -eu

source "$(dirname "$(realpath "${0}")")"/../global-vars.inc.bash

source "${PROJECT_PATH}"/bin/ci/dockerise.inc.bash

phpdd \
    --ansi \
    "${PROJECT_PATH}"/config \
    "${PROJECT_PATH}"/src
