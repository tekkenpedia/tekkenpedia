services:
    _defaults:
        autowire: true
        autoconfigure: true
        bind:
            $projectDir: "%kernel.project_dir%"
            $copyMoveId: "%env(bool:APP_COPY_MOVE_ID)%"

    App\:
        resource: '../src/'
        exclude:
            - '../src/Kernel.php'
