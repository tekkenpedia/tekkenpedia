services:
    _defaults:
        autowire: true
        autoconfigure: true
        bind:
            $projectDir: "%kernel.project_dir%"

    App\:
        resource: '../src/'
        exclude:
            - '../src/Kernel.php'
